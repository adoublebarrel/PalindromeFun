var p = function(p){
    p.isPalindrome = function(s, x1, x2)
    {
        if (q.isS(s) === false) {
            return false;
        }
        
        return true;
    
    }
    return p;
}(p || {});
